{% extends 'base.html.twig' %}
{% block title %} {{ parent()}} | Homepage {% endblock %}
{% block body%}
<h1>{{ title }}</h1>
<p> TODO: fill track-list</p>
{# comment #}
<div>
    <ul>
        {% for track in tracks  %}
        <li>{{ track['song'] }} - {{ track['artist'] }}</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}